#include <list>

using namespace std;

/**
 * @brief Initialise une liste d'entiers avec 8 éléments.
 * 
 */
void listeEntiersInit (list<int> &l);

/**
 * @brief Affiche les éléments d'une liste d'entiers.
 * 
 */
void afficheListeEntiers (list<int> &l);

/**
 * @brief Suppression d'un éléments de la liste avec des itérateurs.
 * 
 */
void listeEntiersRemove (list<int> &l, int pos);